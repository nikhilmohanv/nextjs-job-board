/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: "gqlqkotqzsofzlqh.public.blob.vercel-storage.com",
        },
      ],
    },
  };
export default nextConfig;
